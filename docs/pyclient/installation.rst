Installation
=============

Since the Python client is bundled together with the Proxy-Server in one module,
the installation process is the same for both.

Have a look at the :doc:`Proxy Server installation <../pyserver/installation>`.

