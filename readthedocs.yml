# additionally build PDF & ePub
formats:
    - epub
    - pdf

requirements_file: docs/requirements.txt

build:
    image: latest

python:
   version: 3.5
   pip_install: true
   extra_requirements: 
       - dev

